# 系统测试报告：2025 B 题

## 结论

**系统测试通过。**

程序可从四份原始附件完整运行至厚度结果、诊断指标、置信区间、CSV/JSON 和图片输出；6 项单元测试全部通过，架构验收项均有对应实现和证据。

## 环境

- Python：当前本机解释器
- NumPy 2.3.5
- Pandas 2.3.3
- SciPy 1.16.3
- Matplotlib 3.10.6
- OpenPyXL 3.1.5

## 测试清单

| 类别 | 测试项 | 预期 | 结果 |
|---|---|---|---|
| 正常 | 四附件读取 | 各读取 7469 行并审计异常 | 通过；各排除 1 个不连续零首点，保留 7468 点 |
| 正常 | 波数轴审计 | 严格递增、无重复、间隔稳定 | 通过；约 400.157–4000.122 cm⁻¹，间隔约 0.482 cm⁻¹ |
| 正常 | 双光束公式 | 合成 8 µm 光谱反演误差 <2% | 通过 |
| 正常 | SiC 双角度 | 分别输出厚度且相对差可量化 | 通过；7.9051/7.8296 µm，相对差 0.959% |
| 正常 | 多光束诊断 | 物理、谐波、模型改善联合判定 | 通过；Si 判定为多光束，SiC 不作多光束修正 |
| 正常 | Si Airy 模型 | 两角度结果相容并导出残差 | 通过；3.5785/3.5487 µm，相对差 0.835% |
| 正常 | 不确定度 | 输出标准差和含点估计的 95% 区间 | 通过 |
| 正常 | 端到端入口 | 四附件无异常完成并生成全部文件 | 通过 |
| 边界 | 入射角/折射率/厚度/条纹间距 | 非物理值明确报错 | 通过 |
| 边界 | 合成高密度光谱 | 数千采样点稳定反演 | 通过 |
| 异常 | 文件列不足/波段数据不足 | 抛出带上下文的 `ValueError` | 通过代码路径检查 |
| 异常 | 文件不存在 | 保留 `FileNotFoundError`，不静默生成结果 | 通过 |
| 回归 | `python test_model.py` | 全部单元测试通过 | 通过；6/6 |
| 静态检查 | 最近编辑的 `src` | 无 IDE lint 错误 | 通过 |

## 整体运行记录

```text
命令：cd 25B/src
      python main.py --bootstrap 100
退出码：0
```

生成文件：

- `output/thickness_summary.csv`
- `output/data_audit.json`
- `output/fit_details.json`
- `output/consistency.json`
- `output/sic_10_fit.png`
- `output/sic_15_fit.png`
- `output/si_10_fit.png`
- `output/si_15_fit.png`

## 验收标准核对

1. 四附件读取、单位、排序和异常记录：通过。
2. `model.md` 给出双光束折射角、光程差、相位差和厚度公式：通过。
3. 两个 SiC 角度均有厚度、条纹数、诊断图：通过。
4. 统计误差、折射率系统误差和双角度一致性均有说明：通过。
5. 多光束必要条件覆盖反射率、吸收、相干性、分辨率和厚度均匀性：通过。
6. 每份光谱均输出谐波、AICc、RMSE 和模型判定：通过。
7. Si 两角度 Airy 模型完成拟合和联合汇总：通过。
8. SiC 多光束证据不足，程序保留未修正结果及判定依据：通过。
9. 合成双光束反演误差门槛、单元测试和端到端测试：通过。

## 缺陷与修复循环

### L1-1：非物理参数缺少统一校验

- 现象：零条纹间距或负厚度可能进入底层数值公式。
- 根因：物理公式函数仅依赖上游正常输入，缺少自身防御式校验。
- 修复：在 `optics.py` 增加折射率、入射角、实数折射条件、厚度和条纹间距校验，并新增异常测试。
- 重测：6 项测试全部通过。

### L1-2：重采样区间中心与点估计方法不一致

- 现象：平均间距重采样区间可能不包含 Theil–Sen 点估计。
- 根因：区间中心和点估计使用了不同稳健统计量。
- 修复：保留重采样离散度，并将分布中心校正到最终点估计；重新运行 100 次重采样。
- 重测：四份数据的区间均包含对应点估计。

L1 修复循环计数按一轮实现—重测记为 1/3；无需升级 L2。

## 数据与模型风险

- 附件 2 最大反射率约 102.74%，提示仪器基准/校准可能存在偏差；厚度主要取决于峰位而非绝对反射率，因此不直接裁剪，但已在审计文件保留。
- 折射率仍是主系统误差来源；当前测试只能证明程序和条件模型成立，不能替代晶圆专属光学常数。
- 工作区不是 Git 仓库，无法按流程创建稳定 tag；测试未初始化仓库或修改 Git 配置。

## v2 可视化增强系统测试

### 结论

**v2 系统测试通过。**

### 测试结果

| 测试项 | 验收要求 | 结果 |
|---|---|---|
| 单附件证据图 | 四附件各含原始谱、预处理、拟合、残差、频谱、判据六区 | 通过；4 张均为 3543×2885 |
| 汇总图 | 厚度、角度一致性、证据矩阵、模型质量各 1 张 | 通过；4 张完整生成 |
| 判据可见性 | 显示 0.08、0.12、2%、10 四项阈值及 PASS/FAIL | 通过 |
| 结论一致性 | Si 四项通过；SiC 谐波项失败 | 通过；与 CSV 布尔判定一致 |
| 数值回归 | 四附件最终厚度不变 | 通过；绝对差小于 \(10^{-9}\) µm |
| 图片质量 | 图片不为空，宽 ≥2000、高 ≥1100 | 通过；8/8 |
| 单元测试 | 新旧测试全部通过 | 通过；7/7 |
| 静态检查 | 无新增 lint 错误 | 通过 |

### 完整运行

```text
python main.py --bootstrap 100
退出码：0

python test_model.py
Ran 7 tests in 1.962s
OK
```

视觉复核确认：

- Si 单附件图清楚显示二次谐波、约 0.46 有效反射率及约 19%–20% RMSE 改善，结论为支持多光束。
- SiC 单附件图清楚显示谐波比未过阈值，即使其余指标通过，结论仍为证据不足。
- 厚度汇总按材料拆为两面板，避免 3.5 µm 与 7.9 µm 共用纵轴造成细节压缩。

本轮未发现新缺陷，L1/L2 计数不增加。

## v3 波长—载流子浓度耦合系统测试

### 结论

**v3 系统测试通过。**

新物理模型、情景回退和原有主流程均完整跑通。真实附件未满足“可唯一解释载流子浓度”的全部条件，程序按架构正确触发门控，而不是把更低训练残差误报为更精确结果。

### 测试清单

| 类别 | 测试项 | 验收要求 | 结果 |
|---|---|---|---|
| 正常 | Si 本征色散 | \(N=0\) 时恢复 Edwards–Ochoa 式 | 通过；误差小于 \(10^{-12}\) |
| 正常 | Si/4H-SiC 复介电函数 | 实部折射率为正、吸收非负 | 通过 |
| 正常 | 外延层—衬底薄膜反射 | 反射率有限且位于物理范围 | 通过 |
| 物理退化 | 外延层与衬底介电函数相同 | 结果与层厚无关 | 通过；两个厚度数值一致 |
| 合成反演 | 两角度、已知色散、8 µm 厚度 | 联合恢复误差不超过 2% | 通过 |
| 留段验证 | 连续三波段独立厚度 | 输出 CV、最大偏移并执行门控 | 通过；SiC 未通过后回退，Si 固定高掺杂情景通过 |
| 可辨识性 | Jacobian、相关性、边界和情景比较 | 任一关键条件失败则不报告唯一浓度 | 通过；SiC/Si 均标记浓度不可辨识 |
| 不确定度 | 统计与掺杂系统误差分离 | 分别输出两个区间 | 通过 |
| 异常 | 负浓度、未知材料、数组形状不符 | 明确抛出 `ValueError` | 通过 |
| 异常 | 仅一个角度做联合校准 | 明确拒绝 | 通过 |
| 回归 | 原双光束、多光束与可视化 | 旧测试和模型判定不受破坏 | 通过；SiC 双光束、Si 多光束结论保持 |
| 端到端 | `python main.py --bootstrap 100` | 四附件及全部新旧输出成功生成 | 通过；退出码 0 |
| 静态检查 | 所有改动文件 | 无新增 lint 错误 | 通过 |

### 实际运行记录

```text
python test_model.py
Ran 14 tests in 2.319s
OK

python main.py --bootstrap 100
退出码：0

输出结构与物理边界审计
output-schema-and-physics: OK
```

新增输出：

- `output/dispersion_fit.json`
- `output/refractive_index_curves.csv`
- `output/thickness_summary.csv` 中的色散联合厚度、统计区间、系统区间及浓度可辨识性字段
- `output/consistency.json` 中的材料级色散结果

### 真实附件门控结果

- **SiC**：自由浓度拟合未优于固定情景；固定情景连续波段厚度为约 7.00、8.28、7.99 µm，CV 约 8.69%、最大偏移约 14.99%，未达到 CV≤1%、偏移≤2% 门槛。程序回退约 7.84 µm 的常折射率稳健基线，并保留 7.15–8.33 µm 掺杂系统范围。
- **Si**：最佳固定高掺杂情景的连续波段厚度约为 3.412、3.427、3.441 µm，CV 约 0.42%、最大偏移约 0.78%，稳定性通过；但衬底浓度触及先验上界，因此仅把约 3.41 µm 作为固定情景条件结果，不报告唯一浓度。系统范围为 3.16–3.88 µm。

### 质量判断

系统满足“复杂模型只有通过预测稳定性与可辨识性门控才可采用”的架构要求。本轮没有需要退回实现阶段的 L1 缺陷；模型未通过真实数据门控属于被正确检测的样品信息不足，不是程序故障。

未创建 Git tag：用户未明确授权创建提交或标签。
