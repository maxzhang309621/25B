# 2025 年高教社杯 B 题：碳化硅外延层厚度的确定

## 1. 问题重述与数据

红外光入射到外延层后，外延层表面反射光与外延层—衬底界面反射光发生干涉。题目要求：

1. 在每个界面只发生一次反射、透射的双光束假设下建立厚度模型。
2. 用附件 1（入射角 $10^\circ$）和附件 2（$15^\circ$）计算同一块碳化硅晶圆的外延层厚度并评价可靠性。
3. 推导多光束干涉条件，判断附件 3、4 的硅片光谱是否存在多光束效应，建立模型并计算厚度；同时检查附件 1、2 是否需要多光束修正。

四份附件各有 7469 个原始采样点，波数范围约为 $399.675$–$4000.122\ \mathrm{cm^{-1}}$，间隔约为 $0.482\ \mathrm{cm^{-1}}$。每份数据第一个反射率均为 0，与紧邻点不连续，程序将其记为导出/测量异常并排除，保留 7468 点。

附件 2 的最大反射率约为 $102.74\%$，说明绝对反射率可能带有仪器参考或基线校准误差。本方案不将其强制裁剪到 $100\%$：双光束厚度主要由峰位决定，多光束拟合则显式允许慢变基线和幅度校准项；该异常同时保留在审计结果中。

## 2. 基本假设

1. 外延层上下表面局部平行，分析光斑内厚度均匀。
2. 空气折射率取 $n_0=1$，入射角相对表面法线定义。
3. 主厚度反演使用弱吸收透明波段；SiC 在约 $797$–$1000\ \mathrm{cm^{-1}}$ 的强声子吸收带不参与主估计。
4. 在所选透明波段，SiC 折射率基线取 $n_{\mathrm{SiC}}=2.55$，Si 取 $n_{\mathrm{Si}}=3.42$。折射率与掺杂、晶向、温度和波长有关，因此最终结果是这些光学常数假设下的条件估计。
5. 光谱基线和条纹振幅可以随波数缓慢变化，但厚度在两个入射角下相同。

## 3. 问题 1：双光束干涉模型

### 3.1 折射角与光程差

设空气中的入射角为 $\theta_0$，外延层折射角为 $\theta_1$，由 Snell 定律

$$
\sin\theta_0=n(\tilde\nu)\sin\theta_1,
\qquad
\cos\theta_1=
\sqrt{1-\frac{\sin^2\theta_0}{n^2(\tilde\nu)}}.
$$

外延层厚度为 $d$，膜内一次往返的光程差为

$$
\Delta L=2n(\tilde\nu)d\cos\theta_1.
$$

以波数 $\tilde\nu=1/\lambda$ 表示，两束光的相位差为

$$
\delta(\tilde\nu)
=4\pi d\,\tilde\nu\,n(\tilde\nu)\cos\theta_1+\phi_r,
$$

其中 $\phi_r$ 是两个界面反射产生的固定相位项。它会改变峰谷对应的整数/半整数级次，但不改变相邻同类极值的间隔。

### 3.2 反射率表达式

双光束总电场为

$$
E=E_1+E_2e^{i\delta},
$$

因而反射光强可写为

$$
R(\tilde\nu)
=A(\tilde\nu)+B(\tilde\nu)\cos\delta(\tilde\nu),
$$

其中 $A,B$ 随波数缓慢变化，快速振荡由 $\delta$ 决定。

定义光学相位坐标

$$
g(\tilde\nu,\theta_0)
=\tilde\nu\sqrt{n^2(\tilde\nu)-\sin^2\theta_0}.
$$

若两个同类极值 $\tilde\nu_a,\tilde\nu_b$ 之间跨越 $\Delta m$ 个干涉级次，则

$$
d=\frac{\Delta m}
{2\left[g(\tilde\nu_b,\theta_0)-g(\tilde\nu_a,\theta_0)\right]}.
$$

若窄波段内折射率近似为常数，相邻同类极值的波数间隔为 $\Delta\tilde\nu$，上式化为

$$
\boxed{
d=\frac{1}
{2\Delta\tilde\nu\sqrt{n^2-\sin^2\theta_0}}
}.
$$

当 $d$ 以 $\mu\mathrm m$ 表示、波数以 $\mathrm{cm^{-1}}$ 表示时，右侧还需乘 $10^4$。

### 3.3 数值算法

1. 读取附件并做有限值、排序、重复波数和首点异常审计。
2. 用短窗口 Savitzky–Golay 滤波抑制点噪声；用长窗口估计慢变基线，得到条纹残差。
3. 在波数域做 FFT，在 $1$–$20\,\mu\mathrm m$ 的物理范围内获得厚度粗值。波数轴几乎等间距，因此不需先转换到不等间距波长轴。
4. 根据粗周期设置最小峰距，以 prominence 和 width 提取峰、谷。
5. 分别把峰序号、谷序号对波数做 Theil–Sen 稳健回归，以抑制少量漏峰或误峰；由回归斜率计算厚度。
6. 在稳健极值解附近做小范围全谱相位精修。若宽波段色散使全谱目标偏向相邻级次，则保留峰谷稳健解。
7. 对峰间距和谷间距重采样，计算条件统计置信区间；另行讨论折射率系统误差。

## 4. 问题 2：碳化硅厚度与可靠性

主分析波段为 $1100$–$4000\ \mathrm{cm^{-1}}$，避开 SiC 强声子吸收带。结果如下：

| 入射角 | 峰序列厚度 (µm) | 谷序列厚度 (µm) | 双光束采用值 (µm) | 条件 95% 区间 (µm) |
|---:|---:|---:|---:|---:|
| $10^\circ$ | 7.8394 | 7.9258 | 7.8826 | [7.6773, 8.1522] |
| $15^\circ$ | 7.8107 | 7.7721 | 7.7914 | [7.5605, 8.0510] |

两个角度采用值的相对差为

$$
\frac{|7.8826-7.7914|}
{(7.8826+7.7914)/2}\times100\%
=1.163\%.
$$

按重采样标准差加权，碳化硅外延层厚度为

$$
\boxed{d_{\mathrm{SiC}}\approx7.83\ \mu\mathrm m}.
$$

### 4.1 可靠性说明

- **内部一致性**：峰、谷两套估计接近，且两个入射角结果相差不足 $1\%$，说明条纹级次识别稳定。
- **采样能力**：波数间隔约 $0.482\ \mathrm{cm^{-1}}$，而条纹间距约 $240$–$250\ \mathrm{cm^{-1}}$，每条条纹有约 500 个采样点，采样分辨率充足。
- **统计误差**：表中区间来自极值间隔重采样，反映噪声、峰位和局部条纹不均匀。
- **系统误差**：由

  $$
  d\propto\left(n^2-\sin^2\theta_0\right)^{-1/2}
  $$

  可知近法向入射时 $\Delta d/d\approx-\Delta n/n$。例如折射率存在 $1\%$ 偏差，厚度也约有 $1\%$ 的反向系统偏差；该误差不能由重复采样消除。

## 5. 问题 3：多光束干涉

### 5.1 必要条件

外延层内第 $j$ 次往返光束相对前一束多乘一个复因子

$$
q=r_{10}r_{12}
\exp(-2\alpha d/\cos\theta_1)
\exp(i\delta),
$$

其中 $r_{10},r_{12}$ 为界面 Fresnel 振幅反射系数，$\alpha$ 为吸收系数。多束光在仪器中形成可观察干涉需同时满足：

1. **界面反射条件**：上下界面反射率均非零，且 $|q|$ 足够大，后续光束不能迅速衰减到噪声以下。
2. **低吸收条件**：一次往返吸收不能过强，即指数衰减项不能接近 0。
3. **时间相干条件**：光源相干长度大于相关光束的往返光程差。
4. **空间相干与偏振条件**：参与叠加的光束在探测面重合，并具有可干涉的相同偏振分量。
5. **分辨率条件**：仪器波数分辨率明显小于自由光谱范围

   $$
   \Delta\tilde\nu_{\mathrm{FSR}}
   \approx\frac{1}{2nd\cos\theta_1}.
   $$

6. **平行与均匀条件**：外延层上下表面近似平行，光斑内厚度起伏造成的相位展宽不能抹平条纹。

只满足“存在后续反射”还不够；必须使其振幅、相干性和仪器分辨率都达到可观察水平。

### 5.2 Airy/Fresnel 多光束模型

外延层内所有往返反射场构成等比级数。对 s、p 两种偏振分别有

$$
r^{(j)}_{\mathrm{tot}}
=\frac{r^{(j)}_{01}
+r^{(j)}_{12}e^{2i\beta}}
{1+r^{(j)}_{01}r^{(j)}_{12}e^{2i\beta}},
\qquad
\beta=2\pi d\tilde\nu\,n\cos\theta_1.
$$

非偏振光的反射率为

$$
R=\frac12\left(
\left|r^{(s)}_{\mathrm{tot}}\right|^2+
\left|r^{(p)}_{\mathrm{tot}}\right|^2
\right).
$$

对称、无吸收情形可化为 Airy 形式

$$
T=\frac{1}{1+F\sin^2(\delta/2)},
\qquad
F=\frac{4R_i}{(1-R_i)^2},
\qquad R_{\mathrm{Airy}}=1-T.
$$

程序用有效界面反射率表征条纹尖锐程度，并用“全局厚度搜索 + 有界局部精修”拟合。多光束判定必须同时具有：

1. 高次谐波功率比支持；
2. 有效反射率支持；
3. 相比双光束模型，AICc 和 RMSE 有显著改善。

### 5.3 多光束对厚度精度的影响

理想无吸收、折射率恒定时，Airy 模型会使条纹更尖锐，但同类极值理论位置仍满足相同的相位级次关系，因此仅由准确极值间隔计算的厚度不一定产生偏差。

实际数据中，多光束会带来以下影响：

- 条纹不再是简单余弦，高次谐波使双光束全谱拟合发生系统残差；
- 有限采样、噪声、吸收和色散会使尖锐峰谷的离散定位偏移；
- 若用错误的正弦模型同时拟合基线、振幅和相位，可能跳到相邻干涉级次；
- 正确 Airy 模型利用完整峰形，通常降低残差并提高厚度估计的可重复性，但参数更多，必须防止过拟合。

### 5.4 硅片诊断与厚度

附件 3、4 在两个角度下的二次谐波/基频幅值比分别约为 $0.196$ 和 $0.222$；Airy 模型相对双光束模型的 RMSE 分别改善约 $11.93\%$ 和 $8.65\%$，有效反射率分别约为 $0.410$ 和 $0.422$。四项证据一致，判定：

$$
\boxed{\text{附件 3、4 存在可观察的多光束干涉。}}
$$

| 入射角 | 双光束厚度 (µm) | Airy 多光束厚度 (µm) | Airy RMSE (%) |
|---:|---:|---:|---:|
| $10^\circ$ | 3.4025 | 3.5979 | 0.6252 |
| $15^\circ$ | 3.4105 | 3.5843 | 0.7173 |

两个 Airy 结果相对差为 $0.835\%$，加权联合结果为

$$
\boxed{d_{\mathrm{Si}}\approx3.59\ \mu\mathrm m}.
$$

若仍使用双光束模型，联合厚度约为 $3.41\ \mu\mathrm m$，相对多光束结果低约 $5\%$，说明忽略多次反射会产生不可忽略的模型误差。

### 5.5 碳化硅是否需要多光束修正

附件 1、2 的二次谐波/基频比分别仅约 $0.019$ 和 $0.037$，未达到程序以合成数据和硅片对照设定的谐波证据阈值。虽然增加 Airy 参数能够降低拟合残差，但拟合有效反射率可能吸收未建模的色散和基线，不能单独证明多光束。

因此对 SiC 采用保守结论：

$$
\boxed{\text{现有数据不足以确认 SiC 中存在显著可观察多光束效应，主结果不作 Airy 修正。}}
$$

## 6. 结果汇总

| 材料 | 最终模型 | 双角度相对差 | 最终厚度 |
|---|---|---:|---:|
| 碳化硅外延层 | v8 色散坐标多峰谷模型 | 1.175% | $\boxed{7.45\ \mu\mathrm m}$ |
| 硅外延层 | Airy 多光束模型 | 0.379% | $\boxed{3.59\ \mu\mathrm m}$ |

这些数值依赖所采用的红外折射率。正式竞赛论文中应将晶圆具体晶型、晶向、掺杂浓度和温度对应的复折射率实测/权威数据代入程序，再更新最终有效数字。

## 7. 代码结构与运行

```text
25B/
├── model.md
├── requirements.txt
├── output/
│   ├── thickness_summary.csv
│   ├── data_audit.json
│   ├── fit_details.json
│   ├── consistency.json
│   └── *_fit.png
└── src/
    ├── config.py
    ├── data_io.py
    ├── preprocess.py
    ├── optics.py
    ├── two_beam.py
    ├── multi_beam.py
    ├── diagnostics.py
    ├── uncertainty.py
    ├── plotting.py
    ├── main.py
    └── test_model.py
```

运行：

```bash
cd 25B/src
python test_model.py
python main.py --bootstrap 100
```

`--fast` 可跳过多光束差分进化全局搜索，仅用于快速检查程序是否能够运行。

## 8. 模型局限

1. 题目未给出晶型、晶向、掺杂浓度、温度及对应折射率，当前折射率为文献基线值。
2. 多光束实现用有效 Airy 精细度描述峰形，未同时唯一反演外延层和衬底的完整复折射率；仅凭四条反射谱无法保证所有光学参数均可辨识。
3. 置信区间主要描述极值定位和条纹间距波动，不包含折射率来源的全部系统误差。
4. 宽波段存在色散和吸收，不能把常折射率全谱余弦拟合作为唯一结果；本方案以稳健极值估计和双角度一致性作为双光束主证据。

## 9. 波长—载流子浓度耦合折射率模型

### 9.1 复介电函数

为响应题目中“折射率与掺杂载流子浓度、红外波长有关”的提示，将外延层与衬底分别写成

$$
\tilde n_j(\tilde\nu,N_j)=\sqrt{\varepsilon_j(\tilde\nu,N_j)},
\qquad j\in\{\mathrm{epi},\mathrm{sub}\}.
$$

Si 的本征背景采用 Edwards–Ochoa 在 $2.5$–$25\ \mu\mathrm m$ 的红外经验式，并叠加自由载流子介电响应：

$$
\varepsilon_{\mathrm{Si}}(\omega,N)
=n_0^2(\lambda)
-\frac{\omega_p^2}{\omega^2+i\gamma\omega},
\qquad
\omega_p^2=\frac{Ne^2}{\varepsilon_0m^*},
\qquad
\gamma=\frac{e}{m^*\mu(N)}.
$$

4H-SiC 使用晶格振子与自由载流子耦合形式：

$$
\varepsilon_{\mathrm{SiC}}(\omega,N)
=\varepsilon_\infty
\frac{\omega_{\mathrm{LO}}^2-\omega^2-i\Gamma_{\mathrm{LO}}\omega}
     {\omega_{\mathrm{TO}}^2-\omega^2-i\Gamma_{\mathrm{TO}}\omega}
-\frac{\omega_p^2}{\omega^2+i\gamma\omega}.
$$

参数基线来自 Tiwald 等对 $700$–$4000\ \mathrm{cm^{-1}}$ SiC 的红外椭偏测量以及 Oishi 等对 SiC 外延层厚度、浓度和迁移率的联合反演。程序使用被动介质平方根分支，保证 $k\ge0$。

### 9.2 外延层—衬底反射

对于复折射率不同的外延层和衬底，定义法向波矢

$$
q_j=\sqrt{\varepsilon_j-\sin^2\theta_0},
$$

分别计算 s、p 偏振的空气—外延层和外延层—衬底 Fresnel 系数。单层总反射振幅为

$$
r^{(s,p)}
=\frac{r_{01}^{(s,p)}+r_{12}^{(s,p)}e^{2i\beta}}
       {1+r_{01}^{(s,p)}r_{12}^{(s,p)}e^{2i\beta}},
\qquad
\beta=2\pi d\tilde\nu q_{\mathrm{epi}},
$$

非偏振反射率取 $R=(|r^{(s)}|^2+|r^{(p)}|^2)/2$。当外延层与衬底介电函数相同时，$r_{12}=0$，模型自动退化为单界面反射且与厚度无关。

### 9.3 双角度联合校准与可辨识性

同一材料的两个角度共享

$$
\left(d,\log_{10}N_{\mathrm{epi}},\log_{10}N_{\mathrm{sub}}\right),
$$

每个角度的低阶基线和增益通过线性最小二乘消元。迁移率、有效质量及声子参数保持为论文/工程先验，避免自由度过多。程序同时检查 Jacobian 条件数、参数相关性、先验边界以及自由浓度拟合是否优于固定掺杂情景；任一关键门槛失败时，不把浓度解释为实测值。

### 9.4 附件结果与正确解释

在当前附件缺少晶型确认、掺杂类型、浓度、温度和偏振信息的条件下，两种材料的浓度点估计均未通过全部可辨识性门控，因此应以掺杂情景范围为主要结论：

| 材料 | 常折射率结果 (µm) | 色散/掺杂情景推荐值 (µm) | 掺杂情景系统范围 (µm) | 浓度可辨识 |
|---|---:|---:|---:|---|
| SiC | 约 7.85 | 约 7.84（留段不稳定，回退基线） | 7.15–8.33 | 否 |
| Si | 约 3.59 | 约 3.41 | 3.16–3.88 | 否 |

SiC 的三个连续波段厚度变异系数约为 8.69%，未通过 1% 门槛，因此程序不采用看似更低残差的 7.20 µm 情景值，而回退到约 7.84 µm 的稳健基线。Si 的高掺杂固定情景留段变异系数约为 0.42%，可作为条件结果，但浓度本身仍因触及先验边界而不可解释为测得值。

这不表示色散模型降低了可信度；相反，它揭示了常折射率单点结果未覆盖的主要系统误差。若获得晶圆的 4H/6H 晶型、n/p 型、外延层及衬底浓度或 Hall/C–V 测量，程序可固定相应先验并显著收窄厚度区间。在这些信息缺失时，v7 将常折射率结果作为条件基线；v8 进一步使用色散坐标多峰谷模型给出名义厚度，同时保留情景系统范围。

主要依据：

1. D. F. Edwards and E. Ochoa, *Applied Optics* 19, 4130 (1980), https://doi.org/10.1364/AO.19.004130
2. T. E. Tiwald et al., *Physical Review B* 60, 11464 (1999), https://doi.org/10.1103/PhysRevB.60.11464
3. S. Oishi et al., *Japanese Journal of Applied Physics* 45, L1226 (2006), https://doi.org/10.1143/JJAP.45.L1226

## 10. 增强反射率浓度反演

### 10.1 数据资格与仪器响应

浓度反演直接使用原始反射率线形，而不是去基线条纹。附件 2 有约 3.51% 的点超过 100%，因此程序将 SiC 标记为 `relative_shape` 模式：允许校正相对线形，但禁止把最终浓度解释为绝对测量值。

对于角度 \(a\)，增强仪器模型写为

$$
R_{\mathrm{obs},a}(\tilde\nu)
=b_a+g_aR_{\mathrm{physics},a}(\tilde\nu)
+s\,z(\tilde\nu)+\epsilon_a,
$$

其中 \(g_a\) 和 \(b_a\) 分别是有界角度增益与偏置，\(s\) 是两个角度共享的缓慢响应。与旧模型相比，删除了每个角度独立的 \(zR\) 交互项，避免仪器基线吸收自由载流子造成的慢变谱形。

### 10.2 厚度—浓度双通道

- \(700\)–\(1200\ \mathrm{cm^{-1}}\)：声子—等离子体与 Reststrahlen 浓度敏感通道，权重提高；
- \(1200\)–\(4000\ \mathrm{cm^{-1}}\)：透明区厚度相位锚点；
- \(1300\)–\(1600\ \mathrm{cm^{-1}}\)：当前单振子模型未完整描述二声子吸收，改为降权而不是完全删除。

厚度先由稳健条纹模型锚定，再依次执行单浓度条件反演和双浓度联合反演。只有厚度未触边、浓度区间闭合、两层相关性足够低且相对固定情景有显著改善时，才允许报告浓度点估计。

### 10.3 轮廓区间

对目标参数 \(\eta=\log_{10}N\) 固定网格值，同时重新优化厚度、另一层浓度和仪器参数，得到轮廓目标函数

$$
\chi^2_{\mathrm{prof}}(\eta)
=\min_{\Theta\setminus\eta}\chi^2(\Theta).
$$

采用 \(\Delta\chi^2\le2.706\) 给出 90% 条件区间。若区间触及搜索边界，则只能报告单侧界限或情景范围。

### 10.4 当前附件结论

增强拟合得到的内部候选值约为：

$$
N_{\mathrm{epi}}\approx6.56\times10^{17}\ \mathrm{cm^{-3}},
\qquad
N_{\mathrm{sub}}\approx2.07\times10^{18}\ \mathrm{cm^{-3}}.
$$

对应的诊断性 90% 条件轮廓范围约为：

$$
N_{\mathrm{epi}}\in
[4.95,7.39]\times10^{17}\ \mathrm{cm^{-3}},
$$

$$
N_{\mathrm{sub}}\in
[3.00\times10^{17},2.07\times10^{18}]\ \mathrm{cm^{-3}}.
$$

但该结果未通过最终门控：

1. 附件 2 的绝对反射率资格不合格；
2. 衬底浓度区间触及搜索下界；
3. 厚度触及稳健锚定区间下界；
4. 自由浓度模型相对最佳固定情景仅改善约 2.76%，低于 10% 门槛。

因此最终等级为 `bounded_scenario`，`reported_epi_carrier_cm3` 和
`reported_substrate_carrier_cm3` 均输出为 `null`。上述浓度只能作为当前仪器校正和物性先验下的候选范围，不能作为晶圆的实测浓度。该审计结论保留；外延层厚度则由后续 v8 色散坐标稳健条纹模型重新确定。

## 11. v7 方案 B：本征色散系统误差与自由浓度审计

### 11.1 三轨结果口径

v7 将折射率处理拆成三个互不覆盖的轨道：

1. **主结论轨**：保持常折射率双光束/多光束模型，给出论文主厚度；
2. **本征色散轨**：不自由反演浓度，只用本征色散及低/中/高固定掺杂情景重新估计厚度，形成折射率系统误差带；
3. **自由浓度审计轨**：保留原联合反演与轮廓分析，但只用于证明浓度是否可辨识，不参与主结果自动选择。

采纳规则是显式的：RMSE 更小不能自动替换主结论。本征色散轨只进入可靠性分析，自由浓度轨只进入模型评价。

### 11.2 本征色散固定情景模型

Si 的本征折射率采用 Edwards–Ochoa 红外经验关系；SiC 采用固定 TO/LO 参数的 4H-SiC 单振子晶格模型。在 `intrinsic` 情景中关闭 Drude 项；在 low、medium、high 情景中使用预先给定的外延层/衬底浓度，不把浓度作为优化变量。

当外延层与衬底采用相同本征介电函数时，完整 Fresnel 模型的层间反射会退化，无法单独由绝对反射幅值识别厚度。因此方案 B 在透明区只利用色散修正后的条纹相位，并用正余弦变量投影吸收未知振幅和相位偏置。双角度共享的唯一关键非线性参数为厚度 \(d\)。

最新结果为：

| 材料 | 主厚度 (µm) | 本征色散厚度 (µm) | 本征/固定情景系统范围 (µm) | 本征相对主值变化 |
|---|---:|---:|---:|---:|
| SiC | 7.83 | 7.507 | 7.334–7.832 | −4.16% |
| Si | 3.59 | 3.605 | 3.584–3.605 | +0.31% |

该范围表示在题目信息不足时，采用不同固定折射率情景对厚度造成的条件变化；它不是统计置信区间，也不表示测得了载流子浓度。

### 11.3 不可辨识证据

自由浓度审计轨保留原模型，并把每项证据写入 `output/audit_identifiability.json`：

- **SiC**：连续波段厚度 CV 为 8.69%，超过 1% 门槛；最大偏移为 14.99%，超过 2% 门槛；自由浓度拟合 RMSE 比最佳固定情景高约 13.50%。增强反演还出现绝对反射率资格失败、衬底浓度轮廓触边、厚度锚定触边以及相对固定情景仅改善 2.76%。
- **Si**：自由联合解的衬底浓度触及先验上界；自由拟合相对最佳固定情景约劣化 2.36%。虽然留段厚度稳定，但浓度点估计仍不能被视为唯一物理解。

因此两种材料均保持 `concentration_identifiable=false`。该结论不是删除增强模型，而是说明现有四条近法向反射谱只足以支持固定情景下的厚度敏感性，不能支持唯一载流子浓度测量。

### 11.4 v7 输出

- `intrinsic_dispersion_fit.json`：本征与三档固定情景厚度；
- `intrinsic_n_curves.csv`：各情景复折射率曲线；
- `audit_identifiability.json`：自由浓度不可辨识证据；
- `refractive_index_comparison.json`：三轨并排结果与明确采纳规则；
- `thickness_summary.csv`：新增 `intrinsic_*`、`audit_*` 与轨道标识列。

## 12. v8 色散坐标多峰谷共享厚度模型

### 12.1 模型建立

v8 保留透明波段筛选，不把“取多个波谷”与“选波段”视为替代关系。先排除强吸收、光学相位坐标不单调和边缘质量不足的点，再提取峰、谷观测。

对于固定折射率情景 \(s\) 和角度 \(a\)，定义

$$
g_{s,a}(\tilde\nu)
=\tilde\nu\sqrt{n_s^2(\tilde\nu)-\sin^2\theta_a}.
$$

同类极值序号与该坐标满足

$$
m=2d_{\rm cm}g_{s,a}(\tilde\nu_m)+c_j,
$$

其中 \(j\) 表示“数据集—峰/谷”序列。四条序列使用独立截距 \(c_j\) 吸收未知绝对干涉级次及峰谷相位差，但共享厚度斜率。相邻 \(g\) 间距相对稳健中位数的整数倍用于恢复可能遗漏的干涉级次。

共享斜率采用稳健损失拟合，随后按残差中位绝对偏差审计异常极值。名义情景使用本征色散；低、中、高固定载流子情景仅用于形成折射率模型系统范围，浓度仍不作为优化变量。

### 12.2 稳定性门控

名义结果需同时满足：

1. 峰、谷厚度相对差不超过 1.5%；
2. 10°、15°厚度相对差不超过 2%；
3. 连续三段厚度 CV 不超过 2%，最大偏移不超过 3%；
4. 有效极值不少于 12，异常剔除比例不超过 20%；
5. 对存在多光束证据的材料，v8 厚度与 Airy 厚度相对差不超过 2%。

门控只决定 v8 是否能作为名义厚度；未通过的候选和残差仍完整保留。

### 12.3 碳化硅结果

SiC 在本征色散情景下得到：

$$
\boxed{d_{\rm SiC}=7.4516\ \mu\mathrm m}.
$$

极值重采样给出的条件统计95%区间为
\([7.4306,7.4827]\ \mu\mathrm m\)。峰、谷子模型分别约为
7.4337 µm和7.4634 µm，相对差0.399%；10°和15°分别约为
7.5000 µm和7.4124 µm，相对差1.175%；连续留段CV为0.738%，最大偏移1.563%。39个合格极值全部保留，名义情景通过门控。

低、中、高固定情景的厚度约为7.4515、7.4488、7.3679 µm。将这些结果与常折射率条件值7.8323 µm共同考虑，得到模型系统范围：

$$
d_{\rm SiC,sys}\in[7.3679,7.8323]\ \mu\mathrm m.
$$

因此，7.4516 µm是本征色散条件下的名义厚度；上述系统范围反映未知折射率情景，而不是统计置信区间。

### 12.4 硅片结果与回退

Si 的 v8 本征候选厚度约为3.3995 µm，但峰谷子模型相对差为4.771%，连续留段CV为3.168%，最大偏移为4.449%，且与Airy多光束联合厚度相差5.419%。这些指标均表明简单极值位置受到多光束峰形影响，未通过v8门控。

因此硅片不采用v8候选，继续使用通过四项多光束证据与双角度一致性检验的结果：

$$
\boxed{d_{\rm Si}=3.5943\ \mu\mathrm m}.
$$

这一分叉说明：色散坐标极值模型适合当前SiC双光束条纹；对于多光束显著的Si，完整Airy峰形模型仍是更合适的主模型。

### 12.5 新增输出

- `extrema_observations.csv`：峰谷位置、显著度、宽度与质量权重；
- `dispersion_extrema_coordinates.csv`：各情景的 \(g\) 坐标与级次；
- `dispersion_extrema_fit.json`：情景厚度和稳定性指标；
- `dispersion_extrema_residuals.csv`：极值级次残差与内点标记；
- `dispersion_extrema_comparison.json`：v8采用/回退决策；
- `raw_evidence/extrema/`：级次回归和相邻 \(g\) 间距原始图。

## 13. v9 分析型证据可视化

为使模型依据能够直接用于论文，v9 在纯原始图之外增加分析型证据图。绘图层只读取既有结果，不重新计算厚度或修改门控。

1. **波段资格图**：在去基线条纹上标出透明区下界、SiC二声子排除区、峰谷和不合格极值，并给出合格极值数量。
2. **级次回归图**：显示10°/15°峰谷四条序列、共享厚度拟合线、名义厚度、统计区间与最终采用状态。
3. **残差图**：显示零残差线、稳健内点带、最大残差及极值剔除比例。
4. **稳定性门控图**：把峰谷差、双角度差、留段CV、最大偏移、剔除率和Airy一致性归一到各自阈值，逐项标明通过或失败。
5. **情景厚度图**：同时展示本征/低/中/高情景、条件统计误差棒、常折射率基线和模型系统范围。
6. **载流子审计图**：显示条件数、参数相关、留段指标及不可辨识原因，明确候选浓度仅用于审计。
7. **最终决策图**：并排展示常折射率基线、v8候选、系统范围和最终采用值，解释SiC采用v8而Si回退Airy。

全部分析图位于 `output/analysis_evidence/`；阈值统一来自 `shared_thickness.V8_THRESHOLDS`，避免图中判据与求解代码不一致。
